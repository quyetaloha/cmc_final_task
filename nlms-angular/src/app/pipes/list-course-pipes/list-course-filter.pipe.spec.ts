import { ListCourseFilterPipe } from './list-course-filter.pipe';

describe('ListCourseFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ListCourseFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
