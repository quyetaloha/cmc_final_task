import { LengthMinuteToTimeFormatPipe } from './length-minute-to-time-format.pipe';

describe('LengthMinuteToTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new LengthMinuteToTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
