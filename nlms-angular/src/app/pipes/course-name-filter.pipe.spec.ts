import { CourseNameFilterPipe } from './course-name-filter.pipe';

describe('CourseNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
